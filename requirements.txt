# Core web framework
fastapi
uvicorn[standard]
python-multipart
python-dotenv

# Database drivers
sqlalchemy
psycopg2-binary
pymongo

# LlamaIndex (0.11.x — ReActAgent.from_tools API)
llama-index-core>=0.10.0,<0.12.0
llama-index-embeddings-huggingface>=0.2.0,<0.4.0
llama-index-vector-stores-chroma>=0.1.0,<0.3.0

# CPU-only PyTorch — embeddings run on CPU, so we skip the ~3.9GB of CUDA libs
# the default (GPU) torch wheel drags in. Cuts each image from ~9GB to ~2GB.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu

# HuggingFace embedding model runner
sentence-transformers

# Vector store
chromadb

# Graph DB
neo4j

# Vertex AI (Gemini) — primary LLM via google-genai SDK + ADC
google-genai>=1.0.0

# LLM API client (Whisper voice transcription only)
groq

# Data
pandas
